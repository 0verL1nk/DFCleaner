name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: amd64
          - os: macos-latest
            platform: darwin
            arch: arm64
          - os: windows-latest
            platform: windows
            arch: amd64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install Linux deps
        if: matrix.platform == 'linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      # Build portable version
      - name: Build
        run: wails build -platform ${{ matrix.platform }}/${{ matrix.arch }} -tags webkit2_41
        env:
          CGO_ENABLED: 1

      - name: Package portable (Linux)
        if: matrix.platform == 'linux'
        run: cd build/bin && tar czf DFCleaner-${{ matrix.platform }}-${{ matrix.arch }}-portable.tar.gz dfcleaner

      - name: Package portable (macOS)
        if: matrix.platform == 'darwin'
        run: cd build/bin && tar czf DFCleaner-${{ matrix.platform }}-${{ matrix.arch }}-portable.tar.gz DFCleaner.app

      - name: Package portable (Windows)
        if: matrix.platform == 'windows'
        run: cd build/bin && Compress-Archive -Path DFCleaner.exe -DestinationPath DFCleaner-${{ matrix.platform }}-${{ matrix.arch }}-portable.zip
        shell: pwsh

      # Build NSIS installer (Windows only)
      - name: Install NSIS
        if: matrix.platform == 'windows'
        run: choco install nsis -y

      - name: Build NSIS installer (Windows)
        if: matrix.platform == 'windows'
        run: wails build -platform ${{ matrix.platform }}/${{ matrix.arch }} -tags webkit2_41 -nsis
        env:
          CGO_ENABLED: 1

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dfcleaner-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            build/bin/*-portable.*
            build/bin/*-installer.*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/**/*
